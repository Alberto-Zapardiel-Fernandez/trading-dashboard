// Riesgo máximo por operación (1% del saldo realizado)
export const RIESGO_PORCENTAJE = 0.01

// Colecciones de Firestore — nombres de las tablas
export const COLECCIONES = {
  OPERACIONES: 'operaciones',
  DCA: 'dca',
  CONFIG: 'config'
}
